The project is for translating word documents from english to arabic, it also works from arabic to english but not as well, below is the workflow pipeline:

word -> json -> mirrored json -> simplified json -> translated json -> word
